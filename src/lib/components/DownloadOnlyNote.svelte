<script lang="ts">
	// Why Firefox and Safari have no backup folder and no Restore button, said in
	// one place because it is said in two: the load-time question and Options both
	// have to explain it, and two copies of a sentence about a browser limitation
	// drift into two different accounts of the same limitation.
	//
	// It names what the user can still do and which runtimes do it for them,
	// rather than stopping at "unsupported" — backups still work here, only the
	// way back is manual.
</script>

<p class="text-amber text-ui-xs">
	Your browser does not support editing in place. Using backups- and restoring them means manually extracting <code>.zip</code> files. The desktop app, Chrome and Edge handle this seamlessly.
</p>
